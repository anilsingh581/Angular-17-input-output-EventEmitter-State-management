import { Routes } from '@angular/router';
import { SignalBasedComponent } from './signal-based/signal-based.component';

export const routes: Routes = [
    {path:'signal-based',component:SignalBasedComponent}
];
